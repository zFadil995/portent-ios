---
description: iOS platform rules - references root rules; platform-specific rules live here
alwaysApply: false
globs: ["**/*.swift", "**/*.xcodeproj/**"]
---

# iOS Platform Rules

Platform-specific rules for `apps/ios/`. Shared rules live in the repo root.

## What to do

- Use root rules under `../../../../.cursor/rules/` (core, general, ui, development, testing) for cross-repo guidance.
- Use this directory (`apps/ios/.cursor/rules/`) for iOS-only rules.
- Follow `docs/architecture.md` for layer and MVVM conventions.
- Ensure parity with Android per `.cursor/rules/core/cross-platform-contract.mdc`.

## References

- **Root rules:** `../../../../.cursor/rules/` (core, general, ui, development, testing)
- **Platform rules:** This directory (`apps/ios/.cursor/rules/`)
- **Architecture:** `docs/architecture.md`
- **Cross-platform contract:** `.cursor/rules/core/cross-platform-contract.mdc`

## Platform-Specific Rules in This Directory

- swift-style.mdc: Swift style and conventions
- See `agent-ios.mdc` in root `.cursor/rules/ui/` for agent protocols
