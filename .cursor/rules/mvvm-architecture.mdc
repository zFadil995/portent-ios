---
description: MVVM architecture for iOS app
alwaysApply: false
---

# MVVM Architecture

- ViewModel owns state and business logic; View renders and forwards actions.
- Domain layer has no SwiftUI/UIKit imports.
- Use `@Observable` ViewModels; inject dependencies via init.
- See `docs/architecture.md` for layer rules.
