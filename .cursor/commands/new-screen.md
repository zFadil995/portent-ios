# New Screen (iOS)

Add a new SwiftUI view to an existing feature.

1. Create View in `Features/<Name>/Presentation/`
2. Extend ViewModel state if needed
3. Add navigation destination
4. Add test stub
