# New Feature (iOS)

Scaffold a feature: `Features/<Name>/Data/`, `Domain/`, `Presentation/`, ViewModel, View, test.

Use `ios-feature-scaffold` skill. Create directories, ViewModel template with @Observable, SwiftUI View template, test template, navigation destination, macOS considerations.
