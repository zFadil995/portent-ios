# Add Endpoint (iOS)

Add API endpoint: Codable DTO, APIClient method, repository method, use case.

1. Add Codable struct in feature Data layer
2. Add APIClient method
3. Add Service/Repository method
4. Add UseCase if needed
5. Update core/api/*.md and docs/api-integrations.md
