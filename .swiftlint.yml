reporter: "github-actions-logging"
excluded:
  - DerivedData
  - .build
  - "**/Generated"
disabled_rules:
  - todo
opt_in_rules:
  - empty_count
  - closure_spacing
  - explicit_init
  - no_magic_numbers
no_magic_numbers:
  allowed_numbers: [0, 1, 100]
line_length:
  warning: 120
  error: 200
# CI trigger
